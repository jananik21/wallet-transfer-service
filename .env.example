# Copy to .env for local docker compose overrides
PORT=8080
POSTGRES_DB=wallet
POSTGRES_USER=wallet
POSTGRES_PASSWORD=wallet
POSTGRES_PORT=5432
DB_POOL_SIZE=30

# Local (non-Docker) Spring Boot defaults
DATABASE_URL=jdbc:postgresql://127.0.0.1:5432/wallet
DATABASE_USERNAME=wallet
DATABASE_PASSWORD=wallet
